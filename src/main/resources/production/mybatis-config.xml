<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="production/jdbc.properties" />
    <settings>
        <!-- 全局映射器启用缓存 -->
        <setting name="cacheEnabled" value="true" />
        <!-- 查询时，关闭关联对象即时加载以提高性能 -->
        <setting name="lazyLoadingEnabled" value="false" />
        <!-- 对于未知的SQL查询，允许返回不同的结果集以达到通用的效果 -->
        <setting name="multipleResultSetsEnabled" value="true" />
        <!-- 允许使用列标签代替列名 -->
        <setting name="useColumnLabel" value="true" />
        <!-- 对于批量更新操作缓存SQL以提高性能  -->
        <setting name="defaultExecutorType" value="REUSE" />
        <!-- 数据库超过25000秒仍未响应则超时 -->
        <setting name="defaultStatementTimeout" value="25000" />
    </settings>
    <typeAliases>
        <package name="armadillo.model" />
    </typeAliases>
    <environments default="production">
        <environment id="production">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driverClassName}" />
                <property name="url" value="${jdbc.url}" />
                <property name="username" value="${jdbc.username}" />
                <property name="password" value="${jdbc.password}" />
                <property name="poolPingEnabled" value="true"/>
                <property name="poolPingQuery" value="select now()"/>
                <property name="poolPingConnectionsNotUsedFor" value="600000"/>
                <property name="poolMaximumActiveConnections" value="50"/>
                <property name="poolMaximumIdleConnections" value="20"/>
                <property name="poolMaximumCheckoutTime" value="30000"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper class="armadillo.mapper.SysUserMapper"/>
        <mapper class="armadillo.mapper.SysCardMapper"/>
        <mapper class="armadillo.mapper.SysNoticeMapper"/>
        <mapper class="armadillo.mapper.SysVerMapper"/>
        <mapper class="armadillo.mapper.UserSoftMapper"/>
        <mapper class="armadillo.mapper.SingleCardMapper"/>
        <mapper class="armadillo.mapper.SingleTrialMapper"/>
        <mapper class="armadillo.mapper.SingleVerifyMapper"/>
        <mapper class="armadillo.mapper.RemoteNoticeMapper"/>
        <mapper class="armadillo.mapper.SoftAdmobMapper"/>
        <mapper class="armadillo.mapper.SoftCustomMapper"/>
        <mapper class="armadillo.mapper.SoftUpdateMapper"/>
    </mappers>
</configuration>
